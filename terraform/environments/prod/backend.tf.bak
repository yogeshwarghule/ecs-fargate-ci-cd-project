terraform {
  backend "s3" {
    bucket         = "assignment-prod-terraform-state"
    key            = "prod/terraform.tfstate"
    region         = "ap-south-1"
    dynamodb_table = "assignment-prod-terraform-locks"
    encrypt        = true
  }
}
